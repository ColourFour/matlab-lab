% MATLAB Lab / MATLAB 实验室 — Project 0, Mission 8
% Save this file as bootcamp_arc.m, then complete the three steps.
% 将本文件另存为 bootcamp_arc.m，然后完成以下三步。

% 1. Create t from 0 to 4 with a step of 1.
% 1. 创建 t：从 0 到 4，步长为 1。

% 2. Calculate h using h = 4*t - t.^2.
% 2. 用 h = 4*t - t.^2 计算 h。

% 3. Plot t against h with lines and circle markers ('-o').
% 3. 使用连线和圆圈标记 ('-o')，以 t 为横轴、h 为纵轴绘图。

xlabel('Time (s)');
ylabel('Height (m)');
title('My first arc');
grid on;
h
